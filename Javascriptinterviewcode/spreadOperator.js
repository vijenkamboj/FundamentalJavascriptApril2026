let nums = [1, 2, 3];

// Copying the array
let copy = [...nums];
console.log(copy); // [1, 2, 3]

// Merging with extra elements
let merged = [...nums, 4, 5];
console.log(merged); // [1, 2, 3, 4, 5]
